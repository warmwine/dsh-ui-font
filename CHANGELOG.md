# 变更记录

本文件记录 dsh-ui-font 的版本变化、新功能和问题修复。

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循语义化版本。

## [0.9.0] - 未发布

### 新增

- **设置页全面重做**：卡片式布局、配色跟随界面明暗主题（浅色/深色模式自动适配）、统
  一的按钮和间距风格
- **快捷键改为键帽样式展示**（如 `Ctrl` `Alt` `F` 三个键帽），录制中高亮提示
- **微调列表改为步进器控件**（−/+ 按钮），每行显示"合计"胶囊 = 全局偏移 + 个体偏移
  的最终生效值，叠加语义一目了然；✕ 一键清除个体偏移
- 微调列表的描述行同时显示插件描述或字号规则数

### 变更

- **明确偏移语义为叠加制**：每个区域的实际缩放 = 全局偏移 + 该区域个体偏移。引擎本
  来就是这么算的，这次把界面呈现改成显式表达（全局 +6、代码 -6 → 代码区标准大小，
  其余全部 +6）

## [0.8.3]

### 修复

- 准星选取时部分区域（如右侧文件预览面板）点击后误判为"主界面（全局）"：归属索引
  只在进入选取时构建一次，而样式注入晚的插件不在索引里。改为悬停时发现未知类名即
  增量补扫新样式表；祖先链查找从 3 层放宽到 10 层，适应嵌套深的面板

## [0.8.2]

### 修复

- **严重**：准星点击后弹出的悬浮条完全不可用——−/+ 按钮点不动、「完成」无效、页面
  其他元素也点不了，只能刷新页面脱身。原因是点击拦截器在捕获阶段吞掉了包括悬浮条
  在内的所有点击。现在悬浮条内部点击正常放行，点击悬浮条以外任意位置则干净退出

### 变更

- 快捷键从写死的 Ctrl+Shift+F 改为用户自定义，默认**不设置**（避免与各处搜索快捷键
  冲突）；设置页新增录制模式，按下组合键即存；未设置时红色提示

## [0.8.1]

### 新增

- 全局快捷键召唤准星选取（当时为固定 Ctrl+Shift+F，后于 0.8.2 改为可配置）。解决设
  置面板是模态框、挡住待选取界面的问题——关掉设置后随时召唤
- 选取器改为完全自包含的 DOM 实现：悬浮控制条不再依赖设置页的 React 树，关闭设置
  面板后照常使用，调整结果通过事件同步回设置页

## [0.8.0]

### 新增

- **🎯 准星选取（Spy++ 式）**：设置页「选取区域」按钮进入，鼠标变准星，悬停高亮 +
  归属提示（插件名 · 当前字号），点击后就地 −/+ 调整该区域字号，即时生效。ESC / 右
  键取消。基于运行时构建的"类名 → 插件"归属索引

## [0.7.1]

### 修复

- 令牌缩放完全失效的根因修复：官方字号令牌（`--dsw-font-*`）声明在 `body` 选择器而
  非 `:root`，引擎此前只读 `:root` 导致只拿到字体族、拿不到任何字号——表现为"字体
  换了但大小不变"。改为收集样式表中所有声明字体令牌的规则（不限选择器），输出
  body 级覆盖

## [0.7.0]

### 新增

- 全局字号偏移上限从 +8 提高到 +20
- 启动后 1s/3s/8s 三次有界补扫，覆盖惰性注入样式的插件（此前只在启动瞬间扫一次，
  晚注入的插件不生效）。依然没有轮询，收敛后引擎完全静止

## [0.6.0]

### 变更

- 字体下拉列表改为枚举系统真实安装的字体（宿主半边直接解析字体文件的 sfnt name
  表，纯文件读取、跨平台、无子进程），替换此前写死的四个选项。未安装的当前值标记
  "（未安装）"；旧设置键自动迁移到新格式

## [0.5.0]

### 新增

- 微调列表每个插件显示**描述**：宿主半边提供 `POST /api/dsh-ui-font/descriptions`，
  从各 profile 的 node_modules 读取插件 `package.json` 的 description（缺失时回退
  README 首个标题）。修复了 createRequire 在 junction 安装下解析不到兄弟包的问题
  （改为扫描所有 profile 的 node_modules）

### 变更

- 去掉 MutationObserver 后台监视，改为纯手动：启动扫一次 + 打开设置页/点「刷新」才
  重扫。原则：不做用户没要求的后台工作

## [0.4.0]

（此版本号在开发过程中跳过用于内部整理，实际改动并入 0.5.0）

## [1.0.0 计划前的 0.1–0.3 系列]

- 0.1：静态 CSS 覆盖方案（构建产物扫描 + 生成覆盖规则，后退役）
- 0.2：重写为运行时引擎——遍历 `document.styleSheets` 按 `data-plugin` 归属分组，
  运行时发现所有界面插件的字号规则；新增设置页（settings.section 注册）；修复
  `exports.inject` 必须是依赖声明数组而非函数的激活问题（此前设置页静默不出现）
- 0.3：设置页布局与存储优化；`shell`（外壳样式表）作为自有扫描分类命名
